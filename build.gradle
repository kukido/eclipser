buildscript {
    repositories {
        mavenCentral()
    }
}

plugins {
    id "org.jetbrains.intellij" version "0.7.3"
}

apply plugin: 'idea'
apply plugin: 'org.jetbrains.intellij'
apply plugin: 'java'

repositories {
    mavenCentral()
}

intellij {
    version 'IC-2021.1'
    plugins 'maven', 'ant', 'properties', 'java'
    pluginName 'Eclipser'
}

patchPluginXml {
    untilBuild '211.*'
}

group 'org.jetbrains'
version '0.8.8' // Plugin version
